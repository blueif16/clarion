import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Minimal config. The self-hosted demo site is intentionally bare —
  // scripted accessibility flaws (D2) and the LiveKit panel (U1) are added later.
};

export default nextConfig;
